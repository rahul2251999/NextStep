name: Deploy Backend

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'backend/**'
  workflow_dispatch:

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        working-directory: ./backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        working-directory: ./backend
        run: |
          # Add your test command here if you have tests
          echo "Backend tests would run here"

      - name: Build Docker image
        if: false  # Set to true if you want to build Docker image
        run: |
          echo "Docker build would happen here"
          # docker build -t nextstep-backend ./backend

      - name: Deploy to Railway/Render
        if: false  # Enable when you have deployment configured
        run: |
          echo "Deploy to Railway/Render would happen here"
          # Add your deployment commands

